import React from 'react';
import { View, Text, Button } from 'react-native';

const HomeScreen = ({ navigation }) => {
  return (
    <View>
      <Text>LEXSCAN Mobile</Text>
      <Button title="Upload" onPress={() => {}} />
    </View>
  );
};

export default HomeScreen;